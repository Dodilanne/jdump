import type { Location } from "./types";

export const defaultPath = __dirname;
export const defaultFile = "dump.json";
export const defaultLocation: Location = {
  path: defaultPath,
  file: defaultFile,
};
